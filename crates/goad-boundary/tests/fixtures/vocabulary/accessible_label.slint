// Fixture: a forbidden word in an accessible-label. Scanned by
// `vocabulary.rs`'s `a_forbidden_word_in_slint_markup_is_caught`; never
// compiled.
export component Anything {
    accessible-label: "Habit tracker";
}
