//! The local socket a user-owned watcher writes an event to — `SPEC-003`, this
//! slice's `draft-spec.md` until promotion.
//!
//! This phase declares the module and its envelope submodule only. The
//! listener, the accept task, the socket's lifecycle and the reply are
//! PHASE-03's and PHASE-08's.
#![deny(clippy::arithmetic_side_effects)]

pub mod envelope;
