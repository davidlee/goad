# SPEC-NNN: {title}

**Status:** draft | active | superseded by SPEC-NNN
**Kind:** product | technical
**Owns:** <the one capability or component this spec is normative about>

<!-- A spec is evergreen and normative: it describes what is true now, not how
     it came to be true. No changelog, no revision history, no "we used to".
     Amending it requires explicit user endorsement.
     Requirement ids (R-N) are immutable — append, never renumber. Cite another
     spec's requirement qualified: SPEC-003/R-4. -->

## 1. Intent

<!-- The problem, who has it, and what is different once this exists. Two or
     three paragraphs. A reader who knows nothing should be oriented. -->

## 2. Scope

**In scope:**

**Out of scope:**

**Boundaries:** <what abuts this spec and where the seam falls — the other
specs, components, or actors this one hands off to.>

## 3. Principles

<!-- The non-negotiable stances that settle arguments downstream. Two or three.
     Each should be capable of losing an argument for someone. -->

## 4. Requirements

<!-- Normative. MUST / SHOULD / MAY, used in the RFC-2119 sense. Each
     requirement is atomic, observable, and testable — if you cannot say what
     would falsify it, it is a principle, not a requirement. -->

| id | requirement | verified by |
|----|-------------|-------------|
| R-1 | The shell MUST … | §7 |

## 5. Behaviour

<!-- Primary flows, then alternates, then edges and failure modes. Say what
     happens when the other side is absent, slow, or lying. A Mermaid sequence
     or state diagram where behaviour crosses a boundary. -->

## 6. Interfaces & contracts

<!-- Technical specs only — delete for a product spec.
     Exact surfaces: signatures, wire formats, file layouts, exit codes, error
     cases. Also: what this component owns vs. merely uses, and the assumptions
     it makes about its collaborators. -->

## 7. Verification

<!-- How each requirement is known to hold: tests, checks, observations,
     acceptance gates. Every R-N in §4 must appear here or be explicitly marked
     unverified with a reason. -->

## 8. Open questions

<!-- OQ-N, immutable. A spec may ship with open questions; it may not ship with
     hidden ones. -->

## 9. References

<!-- ADRs, policies, other specs, the slices that implemented this, external
     sources. Cite by id. -->
