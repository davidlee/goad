# tests-asserting-proxies

**This is a pointer.** The lesson lives in
[`a-green-test-can-assert-a-proxy.md`](a-green-test-can-assert-a-proxy.md).

Slice 007's and slice 009's artefacts cite it under this name — seven references
across two slices' ledgers and notes — and those files are append-only, so the
citations cannot be corrected in place. This file exists so they resolve.

The one-line version: **an injection pass proves a case can go red, not that it
goes red for the defect the criterion is about.** Slice 004 shipped four green
tests that each asserted something the regression they guarded would have
survived.
