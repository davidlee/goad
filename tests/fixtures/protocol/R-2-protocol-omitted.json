{
  "requirement":  ["R-2"],
  "description":  "A response that omits `protocol` is accepted: brief §8.2's own examples omit it, so requiring it would reject every backend written against the brief.",
  "now":          "2026-08-23T04:12:00Z",
  "input":        { "view": null },
  "expect":       { "accepted": { "canonical": { "view": null, "schedule": null }, "discarded": [] } }
}
