{
  "requirement":  ["R-52", "R-53"],
  "description":  "Two alternatives of one `choice` field sharing an id make the submitted answer ambiguous about which was picked, so the message is rejected — and the error names an alternative id, never an option id, because the two are different namespaces.",
  "now":          "2026-08-23T04:12:00Z",
  "input":        { "view": { "kind": "choice", "title": "How did it go?", "options": [ { "id": "ok", "label": "Fine", "fields": [ { "id": "f", "kind": "choice", "label": "L", "options": [ { "id": "red", "label": "Red" }, { "id": "red", "label": "Also red" } ] } ] } ] } },
  "expect":       { "error": { "DuplicateAlternativeId": { "id": "red", "at": "view.options[0].fields[0].options" } } }
}
