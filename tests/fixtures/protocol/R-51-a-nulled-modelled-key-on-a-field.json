{
  "requirement":  ["R-50", "R-51"],
  "description":  "An explicit `\"min\": null` on a text field asserts nothing, so it means what omitting the key means and the field is accepted. R-50 rejects a `min` a text field actually carries; a null one carries no value to lose.",
  "now":          "2026-08-23T04:12:00Z",
  "input":        { "view": { "kind": "choice", "title": "How did it go?", "options": [ { "id": "ok", "label": "Fine", "fields": [ { "id": "f", "kind": "text", "label": "L", "min": null } ] } ] } },
  "expect":       {
    "accepted": {
      "canonical": {
        "view": { "choice": { "title": "How did it go?", "body": null, "options": [ { "id": "ok", "label": "Fine", "fields": [ { "id": "f", "kind": "text", "label": "L", "hints": {} } ] } ] } },
        "schedule": null
      },
      "discarded": []
    }
  }
}
