{
  "requirement":  ["R-51", "R-18"],
  "description":  "An explicit `\"hints\": null` on a field asserts nothing, so it means what omitting the key means and the message is accepted with no hints. R-18 refuses a nested `hints` key that carries something; a null one carries nothing to lose, and a serializer emitting null for an absent optional is doing nothing wrong.",
  "now":          "2026-08-23T04:12:00Z",
  "input":        { "view": { "kind": "choice", "title": "How did it go?", "options": [ { "id": "ok", "label": "Fine", "fields": [ { "id": "f", "kind": "text", "label": "L", "hints": null } ] } ] } },
  "expect":       {
    "accepted": {
      "canonical": {
        "view": { "choice": { "title": "How did it go?", "body": null, "options": [ { "id": "ok", "label": "Fine", "fields": [ { "id": "f", "kind": "text", "label": "L", "hints": {} } ] } ] } },
        "schedule": null
      },
      "discarded": []
    }
  }
}
