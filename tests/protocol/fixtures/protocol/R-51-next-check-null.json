{
  "requirement":  ["R-51"],
  "description":  "An explicit `\"next_check\": null` means exactly what omitting it means. It is not an invalid value, so nothing is discarded and nothing is reported — serializers in common backend languages emit null for an absent optional as ordinary output.",
  "now":          "2026-08-23T04:12:00Z",
  "input":        { "view": null, "next_check": null },
  "expect":       { "accepted": { "canonical": { "view": null, "schedule": null }, "discarded": [] } }
}
