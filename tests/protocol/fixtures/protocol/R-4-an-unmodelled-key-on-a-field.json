{
  "requirement":  ["R-4", "R-5", "R-18", "R-51"],
  "description":  "A field object is the one level where a key the host does not model is kept rather than dropped: every key this spec does not name is a presentation hint, so it survives into the canonical value as one. Nothing is discarded, and nothing in the host may branch on it.",
  "now":          "2026-08-23T04:12:00Z",
  "input":        { "view": { "kind": "choice", "title": "How did it go?", "options": [ { "id": "ok", "label": "Fine", "fields": [ { "id": "f", "kind": "text", "label": "L", "placeholder": "a line or two" } ] } ] } },
  "expect":       {
    "accepted": {
      "canonical": {
        "view": { "choice": { "title": "How did it go?", "body": null, "options": [ { "id": "ok", "label": "Fine", "fields": [ { "id": "f", "kind": "text", "label": "L", "hints": { "placeholder": "a line or two" } } ] } ] } },
        "schedule": null
      },
      "discarded": []
    }
  }
}
